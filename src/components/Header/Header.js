import React from 'react';
import './Header.css'
const Header = () => {
    return (
        <div>
            <nav>
                <a href="#">Home</a>
                <a href="#">Shop</a>
                <a href="#">About Us</a>
            </nav>
        </div>
    );
};

export default Header;